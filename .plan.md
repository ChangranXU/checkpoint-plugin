# Checkpoint Audit — 2026-06-01 10:12–10:33 run (HEAD 2bbe5e0, pass-12 code) — VERIFIED + CORRECTED

Headline: **ZERO HIGH defects.** Edit-send re-confirmed NOT-A-BUG on fresh executed bytes; plan-mode capture correct end-to-end; PM1 plan-mode-resume holds at LOW (native `--resume` strips inherited typed permission-mode exactly like the plugin); all pass-10/12 fingerprints (N1/N2/N3/N4/N5/uuidv7/F3) re-verify FIXED/HOLD on **executed** bytes. 160 tests pass.

This file was VERIFIED against source + live bytes (9-agent forensic workflow + direct byte checks). **All 8 problems reproduce, but 6 of 8 fixes/severities in the original draft were wrong and are corrected below.** Verdicts per finding: FK1 CONFIRMED (now provider-agnostic), ES3 MED→LOW + unsound-scan corrected, SUB2 fix-unsafe corrected, PM1 no-fix (dead-code claim wrong), ER2 scope corrected (N4 regression avoided), TS1 native-counts corrected, TS2 rule corrected, FK2 DO_NOT_IMPLEMENT.

## Run identity (genuinely new + captured)
- Codex rollouts `~/.codex/sessions/2026/06/01/` from `10-12-06` (019e80f3 base) through `10-33-20` (019e8107-4e01 executed-resume t2); hooks active throughout.
- First **executed resumes** (the plugin's `checkpoint resume` actually run + resulting provider sessions captured) and first **plan-mode** session.
- EXECUTED resumes carry `resumed_ts` proving a live run: t1 `019e8107-15df` `resumed_ts=2026-06-01T02:33:05.762280+00:00`, t2 `019e8107-4e01` `02:33:20.133240+00:00` — distinct from the stale inherited `start_ts` (02:12:07 / 02:12:25). **NOTE (correction):** `resumed_ts` uses `_now()` (isoformat → `+00:00` form), NOT `_zulu_now()` (`Z` form). The original draft wrote `...762280Z`; the actual bytes are `...762280+00:00`.
- Claude: `21b71340` (startup base + 2 real subagents `a4f9c5d9`/`afcd1cf3`), `3f913f6c` (resume + PLAN MODE turns 2–3), executed-resume t3 `aec8d650` (**resumed_from `21b71340` turn 1**, not 3f913f6c).
- t2's raw rollout is 170710B; the original draft said "codex appended the resumed turn." **CORRECTION:** all 48 records carry the single resume ts `02:33:20.130Z` — the 170710B file is the plugin's resume-REWRITE (prepended head meta + re-stamp), NOT a live appended turn. The 31KB over the 139153B source slice is rewrite expansion, not new turns.

## MED — loadable divergence / capture-time phantom

### FK1 (codex AND claude, RESUME-rewriter) — resume inherits source lineage metadata wholesale  [CONFIRMED — now provider-agnostic]
Evidence (verified): t2 `metadata.json` has `source="fork"`, `forked_from_transcript=`GRANDPARENT `019e80f3`, `forked_at_offset=130583` (a clean boundary in the SOURCE `019e80f4-22ed` (139153B) inherited verbatim, but in NEITHER t2's own 170710B file NOR parent `019e80f3` EOF=88227 — overshoots by 42356B), `start_ts=02:12:25.440362+00:00` (== source `019e80f4-22ed` start_ts). t1 has `source="startup"`, `start_ts=02:12:07.702045` (== grandparent, stale). **CRITIC CATCH (draft missed this):** claude t3 `aec8d650` ALSO has `source="startup"` + stale `start_ts=02:14:32.959457` (byte-identical to source `21b71340`). FK1 is **provider-agnostic**, not codex-only. `cli.py:162` prints `metadata.get("source")`, so `list` mislabels every resume as fork/startup.
Root cause: `resume.py:646-673` `_write_resumed_metadata` does `metadata = raw_metadata` then overrides only `session_id`/`resumed_*`/`cwd`/`provider_session_path`; `source`/`forked_*`/`start_ts`/`session_title`/`session_env` left inherited. Shared by both providers.
Severity: MED (low end — the only functional consequence is `list` mislabeling; `forked_*` has no resume-path reader, so the stale offset never drives logic).
Fix: in `_write_resumed_metadata` set `source="resume"`, `start_ts = _now()` (resume moment), and drop `forked_from_transcript`/`forked_at_offset`/`forked_at_record_count` (stale; true lineage already lives in `resumed_from_*`). Covers both providers automatically (claude has no `forked_*`, so the drop is a no-op there). Also clear stale `session_title` (the source title belongs to the source, not the resume).

### ES3 (codex, capture/list display) — fork-carried edit-send rollbacks are invisible  [REAL but RECLASSIFIED MED→LOW; scan corrected]
Evidence (verified): `_edit_send_replaced_turns` → `{2:3}` for in-session `019e80f4-80e2` but `{}` for forks `019e80f4-22ed` and `019e80fa-4686` even though `_rolled_back_count(turn 0)=1` for both (their turn-0 slice DOES carry a `thread_rolled_back` event). The mapping returns `{}` because there is no captured turn with id < 0 to mark dead — the rollback victim lives in the uncaptured inherited prefix.
Root cause: `cli.py` `_edit_send_replaced_turns` builds `dead` only from captured manifest turn_ids; fork-carried victims in the inherited prefix yield an empty set.
Severity: **LOW** (corrected from MED). Display-only; resume is unaffected. For a fork the rolled-back victim is not in the manifest list and cannot be resumed from this session at all, so F2's original "don't mistake a replaced turn for live" footgun does not even apply. The only missing thing is a historical note that the fork inherited pre-fork rolled-back turns.
Fix (corrected): the original draft's Option B (scan `[0:first_start_offset]`) is **UNSOUND** — it misses `019e80f4-22ed` entirely (0 hits there) and undercounts `4686` (the rollback *events* sit at/after the slice start; only victims sit below). Use Option A: when a manifest's `_rolled_back_count` exceeds the number of `dead` turns it could mark (i.e. it rolled back turns that aren't in the captured set), emit a count-free marker `[carries pre-fork rolled-back turn(s)]` on that turn. Guard on `source=="fork"` (only forks carry an inherited prefix; in-session edit-sends like `80e2` already resolve via the normal `dead` path). An accurate count is optional and would require scanning `[0:forked_at_offset]` on the fork's own transcript (yields 22ed=1, 4686=3) — not worth the I/O for a LOW cosmetic.

### SUB2 (claude, CAPTURE-time) — phantom `no_sidechain_file` subagent shells from resume-replay  [REAL; proposed fix UNSAFE — corrected]
Evidence (verified): `3f913f6c--subagent-a2b78a8b` and `--subagent-a0a18620` have `capture_status="no_sidechain_file"`, empty `manifests/`/`blobs/`/`env-snapshots/`, lineage `agent_type=None`, `sidechain_stem=None`. Both ids appear in ZERO real run transcripts (the only grep hits are this audit's own files) and have no sidechain file. The parent `3f913f6c` references DIFFERENT inherited subagent ids `a4f9c5d9`/`afcd1cf3` (3× each in-transcript, `agent_type=general-purpose`, full captures). No `Agent` tool_use exists after the fork boundary. So the phantom shells are orphan `SubagentStop` events from the resume, not real subagents.
Root cause: `claude_code_hook.py:114-126` — a non-None `agent_id` with no resolvable sidechain file falls into the `if ref is None:` branch and writes a `no_sidechain_file` shell.
Fix (corrected): the original draft's "use `_manifest_references_agent` against the parent's manifests" is **UNSAFE** — a structural ordering hazard. The parent turn manifest that contains the spawning `Task` tool_use is only written when that parent turn ENDS, which is always AFTER the child's `SubagentStop` (measured here: parent referencing manifest written ~48s after the real subagent's SubagentStop). So the check would return False for BOTH phantoms AND genuine in-turn fileless subagents (the common case), silently dropping legitimate lineage and regressing P11-ZOMBIE-1. Instead, use a **capture-time discriminator** available in the payload: suppress the shell only when `transcript_path is None` AND `agent_id` is present AND `agent_type` is absent (phantoms carry an `agent_id` but no `agent_type`; a genuine `Task`-spawned subagent always carries an `agent_type`). The existing test `test_claude_subagent_without_sidechain_file_does_not_slice_parent` (payload has `agent_type="Explore"`) still writes a shell — only the phantom shape (`agent_type=None`) is suppressed.

## LOW — cosmetic / latent fingerprints / dead code

### PM1 (claude) — plan-mode resume strips typed permission-mode, MATCHES native (HOLDS at LOW; no fix)  [core CORRECT; cleanup claims WRONG]
Evidence (verified): source `21b71340` has 4 typed `mode`/`permission-mode` records in `[0:43567]` (idx 0/1/25/26); native `--resume 3f913f6c` has **ZERO** typed records in `[0:43567]`. Native strips inherited typed records exactly like the plugin's fork path (`resume.py:1292` strips `_CLAUDE_STRIPPED_FORK_TYPES`). No data loss vs native.
Corrections to the draft:
- `_ensure_permission_mode_record` is **NOT dead code**. It is the legacy (no `source_session_id`) path of `_rewrite_claude_trajectory` and is directly covered by `test_resume.py:2009-2023` (asserts a synthetic permission-mode IS injected for a non-fork resume). Do NOT delete it.
- The "`env/differ.py:101-102` surfaces `Permission: → plan`" claim is **unverified** for this run: `permission_mode` is `None` in every captured env blob, and the differ line is gated on `permission_changed` (current != target), so with both None it emits nothing. The differ reads `EnvironmentState.permission_mode` (from `CLAUDE_PERMISSION_MODE`/SessionStart env), not the transcript's inline `permissionMode`.
Fix: **none.** Plugin is byte-faithful to native. Do NOT speculatively re-emit a typed permission-mode record without a native `--resume-of-a-plan-turn` oracle. (Caveat unchanged: resuming a plan-TURN whose tail `permission-mode=plan` is inside the slice gets stripped; runtime restore of that state is unprovable from this dataset — but native does the same.)

### ER2 (codex) — cwd rewrite alters recorded command history  [REAL LOW; scope corrected to avoid N4 regression]
Evidence (verified): source `019e80f4-22ed` rec22 `function_call_output` (`call_ec9AFKiMt8xq8gAhQ7UMUeyY`) `pwd` output ending `/Users/.../memory-clean/test` is rewritten in t2 rec23 to `/private/tmp/ckpt_resume_test/t2` — a command result that never produced that path. Also rewritten: `function_call.arguments.workdir`, developer "writable roots are …" text, `<environment_context><cwd>`. Claude t3 preserves source-cwd content paths (7 occurrences) — only `_rewrite_claude_trajectory` rewrites `record["cwd"]`, never content. The two providers are inconsistent.
Root cause: `resume.py:1017-1036` `_rewrite_cwd_in_value` recursively walks ALL string leaves (and dict keys, for N4).
F5-safety check: excluding command output + message text does **NOT** regress F5's real safety goal. Codex enforces its OS sandbox from the STRUCTURED `turn_context` `file_system_sandbox_policy`/`permission_profile` entries (which we keep rewriting). The developer "writable roots" text and `<environment_context>` are advisory model context, never re-derived into OS grants on reload; leaving them stale fails closed (model-facing staleness), it does not re-grant writes. Rewriting `function_call_output.output` is genuinely wrong (historical output).
Fix (corrected): the original draft's "scope to turn_context write-roots + session_meta.cwd" is **too narrow and would regress N4** (`test_resume.py:2263` asserts a `patch_apply_end` `changes` KEY — a top-level `event_msg`, not `turn_context` — gets rewritten). Make the rewrite **field-scoped**: rewrite (a) `turn_context` structured sandbox/permission entry paths + `turn_context.cwd`, (b) `session_meta` payload `cwd`, (c) `event_msg` `patch_apply_begin/end.changes` keys (N4); SKIP `response_item` `function_call_output.output`, `function_call.arguments`, and `message.content` text. Aligns codex with claude (leave historical content stale) while preserving the structured write-root rewrite (F5) and the patch_apply key rewrite (N4).

### TS1 (codex) — inherited-prefix timestamps collapse to one ms  [REAL LOW; native counts corrected]
Evidence (verified): t1 = 1 distinct ts across 12 recs (`02:33:05.760Z`); t2 = 1 across 48 (`02:33:20.130Z`). **CORRECTION:** the draft's native counts (4/5/4/10 distinct ms) are WRONG — they fold in genuine post-fork live turns. The inlined-prefix-only distinct-ms counts (isolated by the >50ms gap to the first live turn) are **2 / 3 / 2 / 8** (`019e80f4-22ed`/`32a3`/`74f4`/`4686`). Native packs ~15–46 records per ms (write-time stamping), so it produces a tight multi-ms burst around the fork moment, never a single ms for a large prefix. The plugin's single reused `now` diverges. t1's 12 records in 1 ms is **NOT** reliably distinguishable from native (native packs ~20 recs/ms); only large prefixes like t2's 48 are.
Root cause: `resume.py:898` `now=_zulu_now()` captured once, reused at 908/927/976 for every record.
Fix (corrected twice): the draft's "+1ms per record" would be WRONG (~1 rec/ms, un-native in the opposite direction). The follow-up idea "re-capture `_zulu_now()` per record" is ALSO unworkable — empirically Python writes all 48–111 records sub-millisecond, so per-record wall-clock still collapses to 1–2 distinct ms (no real fix) AND would break the N5 invariant `test_resume.py:2257` (`all body ts == head_ts`) flakily. The workable fix is a DETERMINISTIC index distribution at native density: record *i* gets `base_ms + (i // DENSITY)` ms with `DENSITY≈20` (native packs ~15–46 recs/ms). This (a) keeps small prefixes — incl. the N5 test's 2 body records — on a single ms (offset 0), so the no-inversion invariant holds unchanged; (b) spreads t2's 48 → ~3 ms and 4686-scale 111 → ~6 ms, matching native's tight multi-ms burst (2/8); (c) is deterministic (no flakiness), monotonic, and loads/sorts fine. Lowest-risk realization of the finding.

### TS2 (codex) — `session_index` `updated_at` always 6-digit microseconds  [REAL LOW; rule corrected]
Evidence (verified): plugin entries `...761182Z`/`...132564Z` are always 6-digit. Native 304 entries: distribution is 5-digit (26) and 6-digit (278) only. **CORRECTION:** of the 296 modern uuidv7 native entries, ZERO end in a trailing-zero digit — proving native (serde_json/chrono) trims **ALL** trailing zeros from the fractional part, NOT "a single trailing zero" as the draft claimed. So `.500000`→`.5Z`, `.172580`→`.17258Z`, `.505647`→`.505647Z`. The draft's quoted plugin values `.761182`/`.132564` don't end in zero, so they coincidentally match native and don't demonstrate the bug; the bug only manifests on resume-emitted micros ending in zero(s) (~10% of values).
Root cause: `resume.py:1521-1529` `_zulu_now_us` always formats microseconds `:06d`.
Fix (corrected): `frac = f"{now.microsecond:06d}".rstrip('0')`; emit `....{frac}Z` when `frac` is non-empty, else drop the fraction+dot entirely (`...SSZ`) for the micros==0 edge (native omits it). Strip-one-zero would still diverge ~1% AND create a new never-seen-in-native `…0Z` fingerprint.

### FK2 (codex) — `forked_at_offset` in fork-own coords paired with parent path  [CONFIRMED LOW; DO_NOT_IMPLEMENT]
Evidence (verified): all 4 forks have offset > parent EOF (130583>88227, 135904>99437, 175618>139153, 258311>222701); each is a clean boundary only in the fork's OWN file. ZERO functional readers in `src/` (only `metadata[...] = anchor[i]` assignments in coordinator.py + 7 assertion lines in `tests/test_codex_hook.py`). A second writer (the codex RESUME path, `coordinator.py:88-95`) stores PARENT-EOF coords under the SAME key names — a latent own-vs-parent coordinate ambiguity, but inert (no reader).
Fix: **DO NOT IMPLEMENT.** No functional impact; a rename is pure churn touching 7 test assertions for zero payoff. FK1 already removes the misleading inherited copy on the resume path, shrinking FK2's surface to the (reader-less) fork-capture writer. Leave as-is; documented here for the record.

## Fix sequencing (implement)
GROUP A — correctness / loadable divergence:
1. **FK1** (`resume.py:646-673`) — `source="resume"`, fresh `start_ts`, drop stale `forked_*` + `session_title`. Both providers.
2. **ER2** (`resume.py` `_rewrite_codex_record_cwd`/`_rewrite_cwd_in_value`) — field-scoped rewrite; keep turn_context + session_meta + event_msg patch_apply keys, skip command output/args/message text.

GROUP CAPTURE — capture-time data shape:
3. **SUB2** (`claude_code_hook.py:114-126`) — suppress phantom shells via `agent_type`-absent discriminator (NOT parent-manifest check).
4. **ES3** (`cli.py` `_edit_send_replaced_turns`/`_cmd_list`) — Option-A count-free marker for fork-carried rollbacks, guarded on `source=="fork"`.

GROUP B — fingerprints / cosmetics:
5. **TS2** (`resume.py:1521-1529`) — `rstrip('0')` + empty-fraction handling.
6. **TS1** (`resume.py` `_rewrite_codex_trajectory`) — per-record `_zulu_now()` (low-risk partial mitigation).

NO CODE CHANGE:
7. **PM1** — byte-faithful to native; `_ensure_permission_mode_record` is live + test-covered (keep). Plan-text corrections only.
8. **FK2** — DO_NOT_IMPLEMENT (no readers; FK1 covers the resume copy).

## PASS / holds (verified correct on these bytes)
- **Edit-send NOT-A-BUG**: `019e80f4-80e2` branches v1=turn2 / v2=turn3; executed t2 resume of an edit-send fork is byte-faithful, replaying codex's own `turn_aborted`+`thread_rolled_back` seam, answering only v2.
- **N1 claude linearization**: t3 `aec8d650` = 1 leaf / 0 dangling parentUuids / 1 sessionId; spine matches native on the 23 shared records (source `21b71340` had a DAG at rec16/17, linearized).
- **Codex resume rewrite**: N5 no ts inversion, N2 head-meta key order, uuidv7 (nibble=7), N3 3-digit ms, F3 compact JSON (t1 0/12, t2 0/48 deviations); t2 metas depth-scaled (3), t1 (2).
- **Subagents**: real claude `a4f9c5d9`/`afcd1cf3` captured to EOF (obs_size==manifest end) with `assistant`/`end_turn`; codex `f148`/`f1a8` trailing `task_complete` read-time recovered via `session_boundary` mode; subagent-resume refusal holds.
- **Plan-mode CAPTURE**: `env_0002_turns_0002-0003` `permission_mode="plan"`; turns 2–3 slices have only `Read` (no Write/Edit/ExitPlanMode — plan never approved); `fs_ref` stable.
- **Native fork lineage well-formed**: t3 `forkedFrom` == native; head `forked_from_id` == metadata parent.
